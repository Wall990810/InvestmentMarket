<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>4.1.0</version>
        <relativePath/> <!-- lookup parent from repository -->
    </parent>
    <groupId>org.wall.im</groupId>
    <artifactId>im-observation</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    <name>im-observation</name>
    <description>im-observation</description>
    <url/>
    <licenses>
        <license/>
    </licenses>
    <developers>
        <developer/>
    </developers>
    <properties>
        <java.version>26</java.version>
        <ai.framework.version>1.0.0-Beta0-SNAPSHOT</ai.framework.version>
        <spring.ai.alibaba.version>1.1.2.3</spring.ai.alibaba.version>
    </properties>
    <dependencies>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter</artifactId>
        </dependency>

        <!-- AI智能体框架核心模块 -->
        <dependency>
            <groupId>org.wall.im</groupId>
            <artifactId>im-ai-core</artifactId>
            <version>${ai.framework.version}</version>
        </dependency>
        <!-- AI智能体框架Agent模块（ReActAgent） -->
        <dependency>
            <groupId>org.wall.im</groupId>
            <artifactId>im-ai-agent</artifactId>
            <version>${ai.framework.version}</version>
        </dependency>
        <!-- AI智能体框架监控模块（Langfuse） -->
        <dependency>
            <groupId>org.wall.im</groupId>
            <artifactId>im-ai-observation</artifactId>
            <version>${ai.framework.version}</version>
        </dependency>

        <!-- Spring AI Alibaba DashScope（通义千问模型支持） -->
        <dependency>
            <groupId>com.alibaba.cloud.ai</groupId>
            <artifactId>spring-ai-alibaba-starter-dashscope</artifactId>
            <version>${spring.ai.alibaba.version}</version>
            <exclusions>
                <exclusion>
                    <groupId>io.modelcontextprotocol.sdk</groupId>
                    <artifactId>mcp-core</artifactId>
                </exclusion>
                <exclusion>
                    <groupId>org.springframework.ai</groupId>
                    <artifactId>spring-ai-vector-store</artifactId>
                </exclusion>
            </exclusions>
        </dependency>
        <!-- 安全版本：替代被排除的漏洞传递依赖 -->
        <dependency>
            <groupId>io.modelcontextprotocol.sdk</groupId>
            <artifactId>mcp-core</artifactId>
            <version>1.1.3</version>
        </dependency>
        <dependency>
            <groupId>org.springframework.ai</groupId>
            <artifactId>spring-ai-vector-store</artifactId>
            <version>1.1.8</version>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>
        </plugins>
    </build>

</project>
